# 待办便签（Todo Sticky）需求文档

> 版本：v1.0 草案 · 整理自《需求澄清问卷》填写结果（2026-07-25）

## 1. 项目概述

一款 Windows 桌面端的极简待办事项软件。主界面即一张「桌面便签贴纸」，用户直接在便签内管理待办事项列表，无需主窗口、列表页等多级界面。

- **产品定位**：轻量、常驻桌面、开箱即用的个人待办工具
- **目标平台**：Windows（代码结构预留跨平台能力，本期不实际适配 macOS / Linux）
- **界面语言**：中文
- **分发形式**：绿色便携版，单文件 exe，免安装

## 2. 技术选型

| 项目 | 决策 |
| --- | --- |
| 技术栈 | Tauri（Rust + 系统 WebView） |
| 前端 | 待定（轻量方案，如原生 HTML/JS 或 Vue/React，随 Tauri 模板确定） |
| 数据存储 | JSON 文件，存放于系统用户目录（`%APPDATA%`） |
| 打包 | 单文件便携 exe |

选型理由：安装包体积小（约 3~8MB）、内存占用低、原生支持托盘 / 全局快捷键 / 窗口定制，且天然具备跨平台能力。

## 3. 功能需求

### 3.1 待办管理（核心）

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| FR-1 | 创建待办 | 在便签内直接输入，回车确认；回车后可快速连续添加多条 |
| FR-2 | 标记完成 | 勾选后**划线置灰，保留在原位**，可取消完成 |
| FR-3 | 删除待办 | 单条删除 |
| FR-4 | 编辑待办 | 双击（或行内）编辑已有待办内容 |
| FR-5 | 拖拽排序 | 拖动调整待办顺序 |
| FR-6 | 优先级 | 每条待办可设高/中/低优先级，用颜色标记 |
| FR-7 | 创建时间 | 自动记录，仅用于排序，界面不强制展示 |

待办字段保持极简：**标题文字 + 优先级 + 完成状态 + 创建时间**，无截止日期、无备注。

### 3.2 窗口与外观

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| FR-8 | 便签式无边框窗口 | 无系统标题栏，外观如贴纸；拖动便签本体即可移动位置 |
| FR-9 | 窗口缩放 | 拖动边缘自由调整尺寸，内容自适应换行 |
| FR-10 | 位置与尺寸记忆 | 窗口位置、尺寸自动保存，重启后恢复 |
| FR-11 | 始终置顶 | **默认置顶**，可通过窗口上的图钉按钮切换 |
| FR-12 | 便签配色 | 经典黄色为默认，预设多色可选：黄 / 粉 / 绿 / 蓝 / 白 |
| FR-13 | 字体大小 | 预设档位调节：小 / 中 / 大 / 特大 |
| FR-14 | 设置持久化 | 配色、字体档位、置顶状态等所有设置修改后自动保存到本地 |
| FR-15 | 待办数量显示 | 未完成待办数量显示在窗口标题处（如「待办便签 (3)」）；系统托盘中不显示 |

本期不做：窗口透明度调节、位置锁定、鼠标穿透、Ctrl+滚轮调字体。

### 3.3 系统托盘与关闭行为

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| FR-16 | 关闭不退出 | 点击窗口关闭按钮，仅最小化隐藏窗口，进程保留在托盘 |
| FR-17 | 托盘单击 | 单击托盘图标：切换便签显示 / 隐藏 |
| FR-18 | 托盘右键菜单 | 菜单项：显示/隐藏便签、设置、**退出**（退出进程的唯一入口） |

### 3.4 全局快捷键

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| FR-19 | 呼出/隐藏快捷键 | 全局快捷键呼出或隐藏便签（默认如 Ctrl+Alt+T） |
| FR-20 | 快捷键自定义 | 用户可在设置中修改快捷键组合 |

### 3.5 设置面板

通过托盘右键菜单「设置」打开，包含：

- 便签配色选择（黄/粉/绿/蓝/白）
- 字体大小档位（小/中/大/特大）
- 全局快捷键自定义
- 开机自启动开关（**默认关闭**）
- 数据手动导出（JSON / Markdown）

### 3.6 数据存储

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| FR-21 | 自动保存 | 待办内容、排序、设置发生变更即自动保存，无手动保存按钮 |
| FR-22 | 手动导出 | 支持将待办数据导出为 JSON / Markdown 文件 |
| FR-23 | 存储位置 | 数据与配置文件存放于 `%APPDATA%` 下的应用目录 |

## 4. 非功能需求

- **性能**：低资源消耗，常驻后台内存占用目标 < 50MB，启动时间 < 2 秒
- **稳定性**：常驻托盘运行，异常退出不丢失已保存数据
- **易用性**：全中文界面，交互直觉化，无需阅读说明书
- **兼容性**：Windows 10 及以上（依赖系统 WebView2，Windows 10 1803+ / 11 通常已内置；打包时考虑引导安装）

## 5. 明确不做（本期范围外）

- 多便签窗口
- 云同步 / 多设备同步
- 到期提醒 / 系统通知
- 分类 / 标签 / 多列表
- 富文本 / 图片 / 附件
- 窗口透明度、位置锁定、鼠标穿透
- 一键清除已完成项
- 待办数量显示在系统托盘
- 数据写入自动备份

## 6. 备注与待确认

1. **默认窗口尺寸未指定**：问卷中留空，开发时取合理默认值（建议约 320 × 400 px），用户可自行缩放并被记忆。
2. 前端框架、默认快捷键组合等实现细节在开发阶段确定。
