# v1.1 版本更新内容

## 设置窗口-数据导出区域UI调整

设置窗口内，`数据导出`区域的这三个按钮：导出 JSON、导出 Markdown、打开导出目录，需要优化一下布局：

将三个按钮合并成以下两个按钮：

1. 导出数据--点击导出按钮，显示一个下拉列表，列表中包含 JSON 和 Markdown 两个选项，点击选项导出对应格式的数据
2. 打开导出文件夹--功能跟`打开导出目录`一致，只是换了按钮文案

布局调整：两个按钮在同一行中显示，按钮宽度需要保持一致。

“数据存储位置：xxxx” 这一行内容去掉，不需要显示存储位置信息。

## 待办事项添加/编辑交互优化

### 添加待办事项

添加待办事项时，在待办事项内容输入框的右侧，放一个选取优先级的控件，优先级默认选中中级

### 编辑待办事项

待办事项列表，双击列表项可弹出对话框编辑事项，关于事项列表作如下调整：

1. 鼠标悬浮在列表项上面时，列表项左侧会显示一个拖拽图标，将这个图标稍微调大一点，图标左右两侧的间距也稍微大一点点
2. 双击事项时不要出现选中文本的效果
3. 编辑待办的对话框，将文本输入框替换为文本域